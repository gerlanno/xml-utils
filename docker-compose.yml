version: '3.8'

services:
  xml-utils:
    build: .
    container_name: xml-utils-app
    ports:
      - "8501:8501"
    volumes:
      - .:/app
      - /app/venv
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
